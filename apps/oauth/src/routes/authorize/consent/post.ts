/**
 * POST /authorize/consent — ユーザーが同意画面で「許可」を押した時の処理
 *
 * - approve → 認可コード発行 → redirect_uri にリダイレクト
 * - deny → error=access_denied で redirect_uri にリダイレクト
 *
 * TODO: フェーズ 2-6 で実装する
 */
export {}
